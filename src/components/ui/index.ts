/**
 * Public re-exports for the BadaMangal design-system primitives.
 *
 * Anything you see here is the *canonical* implementation of its
 * pattern. If you find yourself rebuilding a kicker, a card, or an
 * empty state in JSX, reach for these instead.
 *
 * Live demos for every primitive: visit `/design-system`.
 * Written guide: `DESIGN_SYSTEM.md` at the repo root.
 */
export { default as Kicker } from "./Kicker";
export { default as Pill } from "./Pill";
export { default as SectionHeader } from "./SectionHeader";
export { default as Card } from "./Card";
export { default as JaliFrame } from "./JaliFrame";
export { default as EmptyState } from "./EmptyState";
